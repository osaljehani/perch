apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: perch-data
  namespace: perch
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path   # adjust to a StorageClass your cluster offers
  resources:
    requests:
      storage: 1Gi
