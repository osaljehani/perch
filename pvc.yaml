apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: falco-triage-data
  namespace: falco-triage
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
