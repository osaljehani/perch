apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - pvc.yaml
  - secret.yaml          # create from secret.example.yaml, then SOPS-encrypt
  - deployment.yaml
  - service.yaml
